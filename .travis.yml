branches:
  only:
    - master
  
language: erlang
otp_release:
   - 18.1
   - 18.2.1

python:
  - "3.5"

sudo: false

addons:
  apt:
    packages:
      - build-essential
      - dbus
      - python3-dbus
      - python3-pycurl
      - gir1.2-gtk-3.0

install:
  - "make"

script:
  #- "make tests"
  - "make test-report"

notifications:
  email:
    on_success: change
    on_failure: always
